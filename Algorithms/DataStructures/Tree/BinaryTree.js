



class Node {
    constructor(val) {
        this.val = val;
        this.next = null;
    }
}

/**
 * Binary Tree shall have 1 root, each node can have at most 2 children.
 * Unlike a BST, Binary Tree may not be ordered.
 */
class BinaryTree {
    constructor(){
        this.root = null;
        this.left = null;
        this.right = null;
    }

    // TODO: implement push()/pop()
    // Use BST for now
}